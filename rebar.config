{erl_opts, [debug_info]}.

{deps, [opencensus, 
        opencensus_erlang_prometheus, 
        prometheus, 
        prometheus_httpd]}.

{shell, [{apps, [repl]},
         {config, "config/sys.config"}]}.